import 'package:flutter/material.dart';

class PaperSearchPage extends StatelessWidget {
  const PaperSearchPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Paper section"),
      ),
    );
  }
}
